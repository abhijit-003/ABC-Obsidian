export:
- This user to use something outside the file
- default everything is private, when want to user class, function, variable outside the file use export

async:
- makes function asynchronous
- i.e multiple call can execute parallel, rather that pipe execute.
- Faster execution.
=>
- Short syntax to write function
- => works like function function_name()
await
- wait untill execution of API call
- without await it'll return promise instead of actual data
.post
- Post the request to api
- eg. api.post('/admin/login',credentials);